export interface StatusFooterProps {
  session: string;
  term: string;
  classAverage: number;
  year: string;
  averageStudents: number;
  totalStudents: number;
  noCA: number;
  noExam: number;
  failedStudents: number;
}
